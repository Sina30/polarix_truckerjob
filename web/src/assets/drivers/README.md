put driver-slot portrait .png here, named after the slot key, e.g. driver_0.png, driver_1.png ... driver_8.png (see Config.DriverSlots in config/server.lua)
images should be transparent (cut-out NPC portrait) for best fit
without an image the UI falls back to a tabler:user icon
